import React from 'react';
import { Link } from 'react-router-dom';

const NotFoundPage = () => (
    <div>
        Not Fount
        <Link to='/'>Home</Link>
    </div>
);

export default NotFoundPage;
